# workingAllocation

```sh
# install packages
# only for first run or updates in `package.json`
npm install
```

```sh
# build schedule 
npm run build
```

```sh
# start server
# default address is `http://localhost:6612`
npm run serve
```
